<?xml version="1.0" encoding="utf-8"?>
<!--
Copyright (C) 2012 The Android Open Source Project
modified
SPDX-License-Identifier: Apache-2.0 AND GPL-3.0-only
-->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_USER_DICTIONARY" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_USER_DICTIONARY" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application android:label="@string/english_ime_name"
            android:name="helium314.keyboard.latin.App"
            android:icon="@mipmap/ic_launcher"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:allowBackup="true"
            android:defaultToDeviceProtectedStorage="true"
            android:directBootAware="true"
            android:enableOnBackInvokedCallback="true"
            android:localeConfig="@xml/locales_config"
            tools:remove="android:appComponentFactory"
            tools:targetApi="33">

        <!-- Services -->
        <service android:name="LatinIME"
                android:label="@string/english_ime_name"
                android:permission="android.permission.BIND_INPUT_METHOD"
                android:directBootAware="true"
                android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data android:name="android.view.im" android:resource="@xml/method_dummy" />
        </service>

        <service android:name=".spellcheck.AndroidSpellCheckerService"
                 android:label="@string/spell_checker_service_name"
                 android:permission="android.permission.BIND_TEXT_SERVICE"
                 android:exported="true">
            <intent-filter>
                <action android:name="android.service.textservice.SpellCheckerService" />
            </intent-filter>
            <meta-data android:name="android.view.textservice.scs"
                    android:resource="@xml/spellchecker" />
        </service>

        <!-- Activities -->
        <activity android:name="helium314.keyboard.settings.SettingsActivity"
                android:theme="@style/platformActivityTheme"
                android:icon="@mipmap/ic_launcher"
                android:windowSoftInputMode="adjustResize"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- separate activity that's used internally, only necessary in case the launcher icon is disabled on Android < 10 -->
        <activity android:name="helium314.keyboard.settings.SettingsActivity2"
            android:theme="@style/platformActivityTheme"
            android:icon="@mipmap/ic_launcher"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <!-- intent filter for opening .dict files -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="application/octet-stream" android:scheme="content" android:host="*" android:pathPattern=".*\\.dict" />
            </intent-filter>
        </activity>

        <activity android:name=".spellcheck.SpellCheckerSettingsActivity"
                android:theme="@style/platformActivityTheme"
                android:label="@string/spell_checker_service_name"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <activity android:name="helium314.keyboard.keyboard.emoji.EmojiSearchActivity" android:exported="false"
            android:theme="@style/TransparentActivityTheme" android:excludeFromRecents="true" android:noHistory="true"
            android:windowSoftInputMode="adjustResize|stateAlwaysVisible" android:configChanges="orientation|screenSize"/>

        <!-- Broadcast receivers -->
        <receiver android:name="SystemBroadcastReceiver"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.USER_INITIALIZE" />
                <action android:name="android.intent.action.LOCALE_CHANGED" />
            </intent-filter>
        </receiver>

        <receiver android:name="DictionaryPackInstallBroadcastReceiver" android:exported="false">
            <intent-filter>
                <action android:name="helium314.keyboard.dictionarypack.aosp.UNKNOWN_CLIENT" />
            </intent-filter>
        </receiver>

        <!-- for pasting files from internal clipboard -->
        <provider android:name="helium314.keyboard.latin.database.ClipboardContentProvider"
            android:authorities="@string/clipboard_provider_authority"
            android:enabled="true"
            android:grantUriPermissions="true"
            android:exported="false">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/clipboard_provider_path"/>
        </provider>

        <!--
            keyboard-cipher: espone alla chat l'allegato cifrato appena
            prodotto, e nient'altro. `exported=false` con permessi per URI: chi
            riceve l'intent legge quel file finche' dura il permesso, e nessuna
            altra app puo' interrogare il provider.
        -->
        <provider android:name="helium314.keyboard.cipher.CipherFileProvider"
            android:authorities="${applicationId}.cipherfiles"
            android:enabled="true"
            android:grantUriPermissions="true"
            android:exported="false">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/cipher_file_path"/>
        </provider>

        <!-- necessary for sending mail attachments, remove when data gathering phase is done (end of 2026 latest) -->
        <provider android:name="helium314.keyboard.settings.screens.gesturedata.GestureFileProvider"
            android:authorities="@string/gesture_data_provider_authority"
            android:enabled="true"
            android:grantUriPermissions="true"
            android:exported="false">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/gesture_data_path"/>
        </provider>
    
    <!-- keyboard-cipher -->
<!--
  Riceve il testo selezionato in un'altra app (barra di selezione standard) e
  il testo condiviso via share sheet.

  Attributi di sicurezza, tutti necessari per un'Activity che mostra plaintext:

  exported=true      obbligatorio per ricevere intent da altre app. Significa
                     che chiunque puo' mandarci del testo: accettabile, perche'
                     l'Activity si limita a decifrare e mostrare, e non
                     restituisce mai nulla al chiamante (vedi DecryptActivity).

  NIENTE noHistory   la proprieta' — uscendo, il plaintext non e'
                     raggiungibile tornando indietro — c'e' ancora, ma la fa
                     `onStop` nel codice. Il flag chiudeva la finestra anche
                     quando davanti ci mettiamo NOI il selettore "dove salvo":
                     al ritorno non c'era piu' nessuno a scrivere, e l'allegato
                     veniva salvato di zero byte con scritto "salvato".

  excludeFromRecents il sistema salva su disco uno screenshot di ogni Activity
  =true              per la schermata Recenti. Senza questo, il testo decifrato
                     finisce in un file. FLAG_SECURE nel codice copre lo stesso
                     rischio; qui sono ridondanti di proposito.

  taskAffinity=""    tiene l'Activity fuori dal task principale dell'app, cosi'
                     non compare accanto alle schermate della tastiera.

  launchMode         una sola istanza viva: evita piu' finestre che tengono
  =singleTask        plaintext in memoria contemporaneamente.
-->
<activity
    android:name="helium314.keyboard.cipher.DecryptActivity"
    android:label="@string/cipher_decrypt_action"
    android:exported="true"
    android:excludeFromRecents="true"
    android:taskAffinity=""
    android:launchMode="singleTask"
    android:theme="@style/TransparentActivityTheme">

    <!-- Via 2: barra di selezione del testo. Compare solo nelle app che usano
         la barra standard di Android; quelle con menu di long-press
         proprietario (WhatsApp) non la mostrano. -->
    <intent-filter>
        <action android:name="android.intent.action.PROCESS_TEXT" />
        <category android:name="android.intent.category.DEFAULT" />
        <data android:mimeType="text/plain" />
    </intent-filter>

    <!-- Via 3: share sheet. Fallback universale. -->
    <intent-filter>
        <action android:name="android.intent.action.SEND" />
        <category android:name="android.intent.category.DEFAULT" />
        <data android:mimeType="text/plain" />
    </intent-filter>

    <!--
        Allegati cifrati. Le chat mandano un `.kc` come
        `application/octet-stream`, che e' il tipo di qualunque file che il
        sistema non riconosce: comparire per tutti quelli e' il prezzo per
        comparire per i nostri, perche' il tipo MIME e' l'unica cosa su cui un
        filtro di ACTION_SEND puo' discriminare. Chi apre un file che non e'
        nostro legge "questo testo non e' cifrato" e chiude.
    -->
    <intent-filter>
        <action android:name="android.intent.action.SEND" />
        <category android:name="android.intent.category.DEFAULT" />
        <data android:mimeType="application/octet-stream" />
    </intent-filter>

</activity>

<!--
  Scelta esplicita del destinatario, dalla tastiera.

  Stessi attributi di DecryptActivity e per gli stessi motivi: mostra
  fingerprint, quindi FLAG_SECURE e niente miniatura nei Recenti. NON esportata:
  ci si arriva solo dalla tastiera, con il gettone che dice da quale app.

  Finestra trasparente e non tema di dialogo: il riquadro se lo disegna il
  dialogo Compose, lo stesso della scheda contatto. Con un tema di dialogo
  sotto ne comparirebbe un secondo intorno al primo.
-->
<activity
    android:name="helium314.keyboard.cipher.RecipientActivity"
    android:exported="false"
    android:noHistory="true"
    android:excludeFromRecents="true"
    android:taskAffinity=""
    android:launchMode="singleTask"
    android:theme="@style/TransparentActivityTheme" />

<!--
  Gestione contatti, fingerprint e conflitti di chiave.

  NON esportata e NON nel launcher: ci si arriva dalle impostazioni della
  tastiera, come una qualsiasi schermata di HeliBoard. Un'icona nel launcher
  sarebbe un secondo marcatore visibile del sistema, e non serve a niente che
  una voce nelle impostazioni non faccia gia'.

  Stesso tema delle impostazioni: ci si arriva da li' con un tocco, e una barra
  di stato di un altro colore basta a far sembrare di essere usciti dall'app.
-->
<activity
    android:name="helium314.keyboard.cipher.ContactsActivity"
    android:exported="false"
    android:excludeFromRecents="true"
    android:theme="@style/platformActivityTheme" />

    </application>

    <queries>
        <!-- To detect other IMEs -->
        <intent>
            <!-- changed to * as it's supposed to help with finding other keyboard apps, see https://github.com/HeliBorg/HeliBoard/issues/1340 -->
            <!--<action android:name="android.view.InputMethod" />-->
            <action android:name="*" />
        </intent>

        <!-- To detect names of installed apps -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>
</manifest>
